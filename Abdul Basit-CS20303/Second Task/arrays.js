const numbers=[1,2,3,4,5,"SOmeone"];

console.log(`There are ${numbers} elements in the array`);

console.log("Popped elements is: "+numbers.pop());

console.log(`There are ${numbers} elements in the array`);

numbers.push("Entering again");

console.log(`There are ${numbers} elements in the array`);
